# Default values for test-kube-spring-app.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: akudria/test-kube-spring-app

service:
  type: ClusterIP
  port: 80
  internalPort: 8080
