server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
  endpoint:
    health:
      show-details: "always"

spring:
  application:
    name: test-kube-spring-app

  main:
    banner-mode: "off"

  redis:
    host: redis-kube-master
    port: 6379
    password: xHLnnTU0Jc